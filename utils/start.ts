export class start {
    constructor(){
        console.log(Deno.readTextFileSync("./utils/ascii"));
        console.log(`~ Do not use this tool for illegal purposes / I'm not responsible of any damage done by this tool.`);
        console.log(`~ Use it at your own risk !`);
        console.log(`./ Made by: Alice Snow with ❤️ \n`)
    }
}